const initialState = {}

const sidebarReducer = (state = initialState, action) => {
  // start dev only
  if (action) {
    return state
  } // end dev only

  // return state
}

export default sidebarReducer
